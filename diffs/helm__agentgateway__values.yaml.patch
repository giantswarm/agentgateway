diff --git a/vendor/agentgateway/values.yaml b/helm/agentgateway/values.yaml
index f089fc3..d86a3df 100644
--- a/vendor/agentgateway/values.yaml
+++ b/helm/agentgateway/values.yaml
@@ -2,7 +2,7 @@
 imagePullSecrets: []

 # -- Additional labels to add to all resources created by the Helm chart.
-commonLabels: {}
+commonLabels: {}  # @schema additionalProperties: true

 # -- Add a name to the default Helm base release, which is 'agentgateway'. If you set 'nameOverride: "foo", the name of the resources that the Helm release creates become 'agentgateway-foo', such as the deployment, service, and service account for the agentgateway control plane in the agentgateway-system namespace.
 nameOverride: ""
@@ -15,59 +15,70 @@ serviceAccount:
   # -- Specify whether a service account should be created.
   create: true
   # -- Add annotations to the service account.
-  annotations: {}
+  annotations: {}  # @schema additionalProperties: true
   # -- Set the name of the service account to use. If not set and create is true, a name is generated using the fullname template.
   name: ""

-# -- Configure the RBAC permissions created for the controller.
 rbac:
   # -- Restrict namespaced write permissions to these namespaces. The namespaces must already exist. An empty list preserves the default cluster-wide write access. Cluster-wide read permissions and writes to cluster-scoped resources are unaffected.
   # Restricting this list means only Gateways in these namespaces can be used.
   gatewayNamespaces: []

 # -- Add annotations to the agentgateway deployment.
-deploymentAnnotations: {}
+deploymentAnnotations: {}  # @schema additionalProperties: true

 # -- Add annotations to the agentgateway pods.
-podAnnotations:
+podAnnotations:  # @schema additionalProperties: true
   prometheus.io/scrape: "true"
+  application.giantswarm.io/team: bumblebee

 # -- Add labels to the agentgateway pods. Useful for `NetworkPolicy` selectors (e.g. opt-in egress labels on Cilium-based clusters).
-podLabels: {}
+podLabels: {}  # @schema additionalProperties: true

 # -- Set the pod-level security context. For example, 'fsGroup: 2000' sets the filesystem group to 2000.
-podSecurityContext: {}
+podSecurityContext:  # @schema additionalProperties: true
+  runAsNonRoot: true
+  seccompProfile:
+    type: RuntimeDefault

 # -- Set the container-level security context, such as 'runAsNonRoot: true'.
-securityContext: {}
+securityContext:  # @schema additionalProperties: true
+  allowPrivilegeEscalation: false
+  readOnlyRootFilesystem: true
+  runAsNonRoot: true
+  capabilities:
+    drop: [ALL]
+  seccompProfile:
+    type: RuntimeDefault

 # -- Configure resource requests and limits for the container, such as 'limits.cpu: 100m' or 'requests.memory: 128Mi'.
-resources:
+resources:  # @schema additionalProperties: true
   requests:
-    cpu: 100m
+    cpu: 50m
     memory: 128Mi
+  limits:
+    cpu: 500m
+    memory: 512Mi

 # -- Set node selector labels for pod scheduling, such as 'kubernetes.io/arch: amd64'.
-nodeSelector: {}
+nodeSelector: {}  # @schema additionalProperties: true

 # -- Set tolerations for pod scheduling, such as 'key: "nvidia.com/gpu"'.
 tolerations: []

 # -- Set affinity rules for pod scheduling, such as 'nodeAffinity:'.
-affinity: {}
+affinity: {}  # @schema additionalProperties: true

 # -- Configure how controller pods are spread across topology domains, such as nodes or zones.
 topologySpreadConstraints: []

 # -- Set the pod DNS configuration, such as 'options: [{name: ndots, value: "3"}]'. Merged with the DNS settings the kubelet derives from the pod's dnsPolicy.
-dnsConfig: {}
+dnsConfig: {}  # @schema additionalProperties: true

 # -- Configure the agentgateway control plane deployment.
 controller:
   # -- Set the number of controller pod replicas.
   replicaCount: 1
-  # -- Set the number of old ReplicaSets the controller deployment retains for rollback. Leave unset to use the Kubernetes default of 10. '0' is honored and keeps no history.
-  revisionHistoryLimit:
   # -- Set the priority class name for the controller pod.
   priorityClassName: ""
   # -- Set the log level for the controller.
@@ -75,13 +86,18 @@ controller:
   # -- Configure the controller container image.
   image:
     # -- Set the image registry for the controller.
-    registry: ""
+    registry: gsoci.azurecr.io
     # -- Set the image repository for the controller.
-    repository: controller
+    repository: giantswarm/agentgateway-controller
     # -- Set the image pull policy for the controller.
     pullPolicy: ""
-    # -- Set the image tag for the controller.
-    tag: ""
+    # -- Set the image tag for the controller. A release of the Giant Swarm
+    # line of agentgateway (github.com/giantswarm/agentgateway-upstream: the
+    # vendored upstream release rebuilt, scanned and signed there, mirrored
+    # into gsoci by the retagger); empty would fall back to .Chart.AppVersion,
+    # upstream's own build.
+    # renovate: datasource=docker depName=giantswarm/agentgateway-controller registryUrl=https://gsoci.azurecr.io
+    tag: v1.5.1-gs.3
   # -- Configure the controller service.
   service:
     # -- Create the controller Service.
@@ -94,9 +110,9 @@ controller:
       health: 9093
       metrics: 9092
     # -- Service annotations.
-    annotations: {}
+    annotations: {}  # @schema additionalProperties: true
     # -- Extra labels for the Service.
-    extraLabels: {}
+    extraLabels: {}  # @schema additionalProperties: true
     # -- Cluster IP address.
     clusterIP: ""
     # -- Cluster IPs for dual-stack.
@@ -120,7 +136,7 @@ controller:
     # -- Session affinity.
     sessionAffinity: ""
     # -- Session affinity configuration.
-    sessionAffinityConfig: {}
+    sessionAffinityConfig: {}  # @schema additionalProperties: true
     # -- IP families.
     ipFamilies: []
     # -- IP family policy.
@@ -142,7 +158,7 @@ controller:
   #       secretKeyRef:
   #         name: agentgateway-secrets
   #         key: apiToken
-  extraEnv: {}
+  extraEnv: {}  # @schema additionalProperties: true
   # -- Add extra sidecar containers to the controller pod.
   extraContainers: []
   # -- Add extra volume mounts to the controller container.
@@ -162,14 +178,14 @@ controller:
   #   type: RollingUpdate
   #   rollingUpdate:
   #     maxSurge: 100%
-  strategy: {}
+  strategy: {}  # @schema additionalProperties: true
   # -- Set pod disruption budget for the controller. Note that this does not
   #    affect the data plane.
   # E.g.:
   #
   # podDisruptionBudget:
   #   minAvailable: 100%
-  podDisruptionBudget: {}
+  podDisruptionBudget: {}  # @schema additionalProperties: true
   # -- Set horizontal pod autoscaler for the controller. Note that this does not
   #    affect the data plane. The scaleTargetRef is automatically configured to
   #    target the controller deployment.
@@ -185,7 +201,7 @@ controller:
   #         target:
   #           type: Utilization
   #           averageUtilization: 80
-  horizontalPodAutoscaler: {}
+  horizontalPodAutoscaler: {}  # @schema additionalProperties: true
   # -- Set vertical pod autoscaler for the controller. Note that this does not
   #    affect the data plane. The targetRef is automatically configured to
   #    target the controller deployment.
@@ -200,23 +216,25 @@ controller:
   #         minAllowed:
   #           cpu: 100m
   #           memory: 128Mi
-  verticalPodAutoscaler: {}
+  verticalPodAutoscaler: {}  # @schema additionalProperties: true

 # -- Configure the agentgateway data plane deployment.
 proxy:
   image:
     # -- Set the default image registry.
     # Set to override the global value.
-    registry: ""
+    registry: gsoci.azurecr.io
     # -- Set the default image repository.
-    repository: agentgateway
-    # -- Set the default image tag.
-    tag: ""
+    repository: giantswarm/agentgateway
+    # -- Set the default image tag: the same release of the Giant Swarm line
+    # of agentgateway as the controller.
+    # renovate: datasource=docker depName=giantswarm/agentgateway registryUrl=https://gsoci.azurecr.io
+    tag: v1.5.1-gs.3

 # -- Configure the default container image for the components that Helm deploys. You can override these settings for each particular component in that component's section, such as 'controller.image' for the agentgateway control plane. If you use your own private registry, make sure to include the imagePullSecrets.
 image:
   # -- Set the default image registry.
-  registry: cr.agentgateway.dev
+  registry: gsoci.azurecr.io
   # -- Set the default image tag.
   tag: ""
   # -- Set the default image pull policy.
@@ -245,7 +263,7 @@ discoveryNamespaceSelectors: []
 #      agentgateway:
 #        name: shared-gwp
 #        namespace: kgateway-system
-gatewayClassParametersRefs: {}
+gatewayClassParametersRefs: {}  # @schema additionalProperties: true

 # -- Name of the primary GatewayClass the controller creates and manages.
 #    Leave empty to use the built-in default ("agentgateway"). Sets AGW_AGENTGATEWAY_CLASS_NAME.
@@ -288,7 +306,7 @@ monitoring:
     # -- Scrape interval for the controller ServiceMonitor and the proxy PodMonitor.
     interval: 15s
     # -- Additional labels to add to the controller ServiceMonitor and the proxy PodMonitor (e.g. release: prometheus).
-    extraLabels: {}
+    extraLabels: {}  # @schema additionalProperties: true

   proxy:
     podMonitor:
@@ -300,7 +318,7 @@ monitoring:
     gatewayClassNames:
       - agentgateway
     # -- Namespace selector used by the proxy PodMonitor. Defaults to the release namespace only.
-    namespaceSelector: {}
+    namespaceSelector: {}  # @schema additionalProperties: true

   grafanaDashboard:
     # -- Create the Grafana dashboard ConfigMap.
