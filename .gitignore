vendor
.vendir*

# Subchart archives regenerated by `helm dependency update`. The dependency is
# a file:// path into charts/, so the archive is redundant and goes stale on
# every vendir bump.
helm/*/charts/*.tgz
